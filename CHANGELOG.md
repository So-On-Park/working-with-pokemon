# 업데이트 내역 (Changelog)

Pokemon Buddy의 버전별 변경 사항이야. 업데이트하는 방법은 [UPDATING.md](UPDATING.md)를 봐줘.

---

## v1.3.1

**성장 속도 대폭 상향 (밸런스 개편)**
- 레벨 곡선을 **균일 곡선**으로 변경 — 모든 레벨이 동일하게 EXP 200 (기존: 레벨×100으로 갈수록 느려짐). 최대 레벨 100.
- **화면에 켜두기만 해도** 15분마다 EXP +50 · 친밀도 XP +25 (기존 +1/+1). 하루 종일 함께하면 **시간당 1레벨 · 친밀도 +1**, 약 2주(화면시간 100시간)면 레벨·친밀도 모두 만렙.
- 밥 주기도 이제 EXP +10 (기존: 친밀도만).
- **친밀도 감소(방치 패널티) 완전 제거** — PC를 꺼두거나 주말을 쉬어도 친밀도는 그대로.

**확인·수정**
- **현재 버전 표시** — 트레이 우클릭 메뉴 맨 위와 트레이 아이콘 툴팁에서 설치된 버전을 바로 확인 가능.
- 만렙(100) 도달 후 친밀도 100을 찍어도 수집광 스킬을 깨우치지 못하던 문제 수정 (레벨업 없이도 체크).
- 야생 포켓몬을 잡을 때 실제로 던진 몬스터볼 종류가 정확히 소모·기록되게 수정.
- 포켓몬 불러오기 창이 첫 실행(더블클릭) 시 버디 뒤에 숨던 문제 수정, 보내기 확인을 한 번만 묻게 정리.
- 아이템 보유량 상한(999)을 저장 단계에서도 강제.
- 기능 설명 창을 더 간결하게 정리, 출퇴근 시간표 기능 제거(환영 인사로 대체).

---

## v1.3.0

**포켓몬 / 스킬 보내기·불러오기 (신규)**
- 포켓몬을 **단일 파일(`.pokeball`)로 내보내** 친구에게 전달. 레벨·경험치·친밀도·성격·성별·닉네임·배운 스킬까지 그대로 담겨가고, 커스텀은 GIF도 동봉돼. (보내면 내 가방에선 사라지는 진짜 전송)
- 스킬 교본은 **`.scroll` 파일**로 주고받기.
- **불러오기**: 트레이 메뉴 또는 파일 더블클릭(설치 시 파일 연결 등록) → **두근두근 → 몬스터볼 흔들·열림 → "따단! ○○이 동료가!"** 연출. 도감에 없던 종이면 도감에 추가. 한 번 불러온 파일은 자동 삭제(중복 방지).
- 종(species) 전용 파일을 열면 새로 잡은 것처럼 등장(레벨1·랜덤). 받은 `.pokeball`/`.scroll` 파일은 누구나 불러올 수 있어.

**스킬 추가**
- 🎯 **명포수** — 야생 포켓몬이 나타나면 자동으로 몬스터볼을 던져 잡아줘. 레벨 100이 되면 스스로 깨우쳐.
- 수집광: 끌어당겨 주운 버디가 직접 멘트. 여러 마리가 가졌으면 파티 순서(1→2→3)대로 적용.

**아이템 / 가방**
- 음식 22종·장난감 25종으로 대폭 확대(🍕🍔🥗🍣 · ⚾🎻🧸🎨 등), 밥주기·놀아주기 때 **랜덤**으로 골라 주고 아이템마다 **다른 멘트**.
- 가방: 음식·장난감·몬스터볼을 **한 행에 대표 아이콘 + 합계(최대 999)** 로 정리. 4열 가운데 정렬, 설명은 마우스 호버로.

**다듬기**
- 전체 폰트 크기 통일(조금 작게), **시스템 안내문 존댓말화**(포켓몬 말풍선은 그대로 반말).
- ⓘ 아이콘 깨짐 수정, 전송 확인창이 가방 창 뒤에 숨던 문제 수정.

---

## v1.2.1

**첫 포켓몬 선택(온보딩) 개선**
- 몬스터볼을 누를 때마다 랜덤으로 바뀌던 동작 제거 → **각 볼에 포켓몬 고정**(볼1 이상해씨 · 볼2 파이리 · 볼3 꼬부기 · 볼4 피카츄). 누가 나올지는 **실루엣**으로 가려져 있다가, "이 포켓몬으로 하시겠습니까?" 확인 시 공개돼.
- 실루엣 크기를 조금 줄여서 화면에 더 자연스럽게.

**설치 경험 개선**
- 설치 중 Pokemon Buddy가 실행 중이면 **"종료할까요?" 안내 후 자동 종료**하고 이어서 설치 (진행도는 자동 저장되어 안전).
- 설치 파일명에 버전 표기 (`PokemonBuddy-Setup-1.2.1.exe`).
- 실행 파일(.exe) 아이콘이 메타몽으로 안 보이던 현상(아이콘 캐시) 정리.

---

## v1.2.0

데스크탑 버디의 큰 업데이트. 버그 리포트 반영 + 신규 기능 + 배포 방식 개선.

**신규 기능**
- 📜 **스킬 시스템** — 아주 가끔 두루마리(스킬 교본)가 화면에 떨어져. 주워서 가방에 보관하고, **내 가방 → 전수**로 파티원에게 기술을 가르칠 수 있어.
- 🧲 **수집광 스킬** — 이 기술을 배운 파티원은 화면에 떨어진 아이템을 잠시 뒤 **끌어당겨 자동으로 주워와**. 친밀도가 **100**인 포켓몬은 레벨업할 때 스스로 깨우치기도 해.
- 🎚️ **화면 아이템 표시 on/off** — 트레이 메뉴에서 화면 드롭을 끄고 켤 수 있어 (가방은 그대로 유지).
- ⓘ **설명 아이콘** — 내 가방·기술 항목 옆 ⓘ를 호버/클릭하면 효과 설명이 떠. 화면이 한결 깔끔해졌어.

**버그 수정 / 개선**
- 친밀도가 이미 100이면 친밀도 아이템(상처약류)을 쓸 수 없게 막고, "이미 최고치" 안내.
- 출근 후 실행 시 리마인더가 한꺼번에 쏟아지던 문제 수정 (실행 시점부터 주기 재시작).
- 밥 주기·놀아주기로 아이템을 쓰면 가방 수량이 **즉시** 반영.
- 표시 크기 배율 **최대 3배**까지 확대 + 큰 배율에서 가장자리가 잘리지 않도록 창 크기 자동 조정.
- 다른 창을 누르면 버디가 작업표시줄 아래로 가라앉던 현상 완화 (주기적 재배치).
- 화면에 떨어진 아이템이 사라지는 시간을 **5분 → 90초**로 단축.
- 실행 파일 아이콘을 **메타몽**으로 추가.

**배포 / 데이터**
- 🪟 **설치 파일(Setup.exe) 배포 지원** — 압축 해제 대신 설치하면 "프로그램 추가/제거"에 등록되고, 시작 메뉴·바탕화면 바로가기, (선택) 부팅 자동 실행까지 한 번에.
- 💾 **업데이트해도 진행도 유지** — 세이브 데이터를 설치 폴더 밖(`%LOCALAPPDATA%\PokemonBuddy`)에 보관해서, 새 버전을 설치/덮어써도 키우던 포켓몬이 그대로야. 기존 설치본의 데이터는 처음 실행할 때 자동으로 옮겨와.

---

## v0.1.x (초기 릴리스)

- 데스크탑 위 포켓몬 파티(최대 3마리), 친밀도·경험치·레벨업·진화.
- 야생 포켓몬 발견·포획, 도감.
- 아이템 드롭·가방·특수 아이템 사용.
- GIF로 커스텀 포켓몬 등록.
- 리마인더 알림.
- 백업 / 복원 / 초기화.
